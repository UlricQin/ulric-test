搞WEB的一定要懂HTTP协议

当前用的比较多的是1.1版本

http协议是基于tcp协议的，默认端口是80

http方法：
get 从服务器获取一个资源（一般是网页）
post 向服务器提交数据（可能会创建一个新的资源）
put 向服务器提交数据
delete 删除指定资源
head 只请求网页的头部信息

http通过状态码来标示返回信息，常见的如：
200 正常，请求成功
301 永久移动，一般用于域名重定向
304 未修改，一般用于缓存
401 禁止访问，未授权
403 禁止访问，通常代表已认证通过，但是没有访问权限
404 未找到资源
500 服务器内部错误


